# 목적: 로컬/배포 환경에서 필요한 환경변수 이름을 팀과 공유하기 위한 양식
# 왜: 실제 비밀값(.env)은 git에 올리면 안 되므로, 값이 빈 사본을 대신 공유함
# 무엇: 이 파일을 복사해 .env로 만든 뒤, 비어있는 값만 각자 채워서 사용
# 사용법: cp .env.example .env

PORT=3000

DB_HOST=localhost
DB_PORT=3306
DB_USER=
DB_PASSWORD=
DB_NAME=iksanshop

SESSION_SECRET=
